version: "3.5"

services:
  app:
    build: literal
    volumes:
      - ./literal/:/app/
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8010:8000"
    expose:
      - 8010
    env_file:
        - ./variables/dev.env

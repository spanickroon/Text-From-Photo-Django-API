name: Django CI/CD

on: [push]

jobs:
  build:
    name: Build Docker Images
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build images
        run: |
          make start

  linting:
    name: Linting
    runs-on: ubuntu-latest
    needs: [build]

    steps:
      - name: Pylint
        run: |
          make pylint
      - name: Mypy
        run: |
          make mypy
      - name: Black
        run: |
          make black

  testing:
    name: Testing
    runs-on: ubuntu-latest
    needs: [build, linting]

    steps:
      - name: Test coverage
        run: |
          make test-coverage